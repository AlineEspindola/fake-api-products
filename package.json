{
  "name": "fake-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch api-products.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
